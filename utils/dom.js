/**
 * utils/dom.js
 * -------------------------------------------------------------
 * Pequeños ayudantes de acceso al DOM. Sin lógica de negocio.
 * -------------------------------------------------------------
 */

/** Atajo de document.querySelector. */
export const $ = (selector) => document.querySelector(selector);
